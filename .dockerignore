# Keep the Docker build context small. `dist/` is intentionally allowed — the
# runtime image is assembled from the prebuilt binaries placed there.
/target
/vendor
/runtime
/docker-out
/.git
.phpunit.cache
.phpunit.result.cache
composer.lock
